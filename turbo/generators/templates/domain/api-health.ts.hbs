import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({
    status: 'ok',
    service: '{{ dashCase name }}-mock-server',
    timestamp: new Date().toISOString(),
  });
}
